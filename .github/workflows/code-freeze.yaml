# name: Code Freeze

# on:
#   workflow_dispatch:

# jobs:
#   create-pull-request:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout the repository
#         uses: actions/checkout@v2
#         with:
#           ref: dev  
#           token: ${{ secrets.GIT_TOKEN }}

#       - run: git config user.name "nuxeo-webui-jx-bot" && git config user.email "webui@hyland.com"
      
#       - name: Create Pull Request
#         id: create_pr
#         uses: peter-evans/create-pull-request@v5
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           commit-message: Merge dev into main
#           branch: code-freeze/${{ github.sha }}
#           base: main
#           title: "Sync dev to main"
#           body: |
#             This pull request was automatically generated by the code freeze workflow.
#             Please review and merge these changes.

#       - name: Output Pull Request URL
#         run: echo "Pull request URL: ${{ steps.create_pr.outputs.pull-request-url }}"